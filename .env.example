# =============================================================================
# Local AI Orchestrator - Environment Configuration
# =============================================================================

# OpenRouter API key for model fetching
OPENROUTER_API_KEY=your_openrouter_key_here

# Database URL (SQLite for dev, PostgreSQL for production)
DATABASE_URL=sqlite:///data/orchestrator.db

# Scheduler settings
SCHEDULER_TIMEZONE=UTC

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# API Authentication (optional - leave empty for no auth)
API_KEY=

# =============================================================================
# Redis Cache Configuration (optional)
# =============================================================================

# Redis connection URL (leave empty to use in-memory cache)
# Examples:
#   redis://localhost:6379/0
#   redis://:password@localhost:6379/0
#   rediss://user:password@host:6380/0 (SSL)
REDIS_URL=

# Cache backend: "memory" (default) or "redis"
CACHE_BACKEND=memory

# Default cache TTL in seconds (1 hour)
REDIS_TTL_SECONDS=3600

# Key prefix for Redis (useful for shared instances)
REDIS_PREFIX=orchestrator:

# =============================================================================
# Docker Compose Ports (optional overrides)
# =============================================================================

# API server port
API_PORT=8000

# Frontend dashboard port
FRONTEND_PORT=3000

# Redis port (if using docker-compose)
REDIS_PORT=6379

# =============================================================================
# Resilience Settings
# =============================================================================

# Enable offline cache fallback when adapters fail
OFFLINE_MODE_ENABLED=true

# Days to retain metrics before pruning
METRIC_RETENTION_DAYS=30
